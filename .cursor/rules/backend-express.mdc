---
description: 主后端 Express + MongoDB TypeScript 约定（express-mongoDB）
globs: backend/express-mongoDB/**/*.ts
alwaysApply: false
---

# 后端（express-mongoDB）

## 架构分层

- **入口**：`server.ts`（开发 `npm run dev` / `dev:watch`；生产先 `npm run build` 再 `npm start` 读 `dist/`）。
- **路由**：`src/routes/*.ts` — 只负责参数解析、调用服务、返回 HTTP 结果。
- **业务**：`src/services/*.ts` — 核心业务与对 Mongoose 的编排。
- **模型**：`src/models/*.ts` — Schema 与数据访问边界清晰。
- **中间件**：`src/middleware/auth.ts` — JWT 等横切逻辑；受保护路由必须显式挂载。

## 数据与安全

- 连接串、密钥等放在 **`.env`**（勿提交）；`dotenv` 已在应用中使用。
- 密码使用 **bcryptjs**；令牌使用 **jsonwebtoken**，与前端 `Authorization: Bearer` 约定一致。

## 脚本与数据

- `scripts/` 下为初始化、CSV 同步等一次性或运维脚本；改动时保持与当前 `package.json` 的 `scripts` 名称一致（如 `init`、`sync-csv`）。

## 风格

- 新增接口保持 **REST 风格** 与现有路由命名习惯；错误响应结构尽量与前端 `http.ts` 拦截器、已有页面处理兼容。
- 注释与用户向日志说明使用**简体中文**。
