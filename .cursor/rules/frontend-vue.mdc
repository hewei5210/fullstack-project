---
description: 主前端 Vue 3 + TypeScript 约定（vite-vue3-ts）
globs: frontend/vite-vue3-ts/**/*.{vue,ts}
alwaysApply: false
---

# 前端（vite-vue3-ts）

## 技术栈与工具

- Vue 3 **Composition API** + `<script setup lang="ts">` 与现有页面风格一致。
- UI：**Element Plus**；路由：`src/router/index.ts`；全局入口：`src/main.ts`。
- 构建：**Vite 6**；类型检查：`vue-tsc`（`npm run build`）。

## 网络与认证

- 所有业务请求应走 **`src/net/http.ts`** 封装的 Axios 实例，以保持 JWT、`/login`/`/refresh` 例外与 401 刷新逻辑一致。
- `baseURL` 使用 `import.meta.env.VITE_APP_API_BASE`，本地联调勿写死生产域名。

## 新增页面流程

1. 在 `src/views/` 下新增页面（可按模块分子目录，与现有 `i18n/`、`user/` 等一致）。
2. 在 `src/router/index.ts` 注册路由；需在主导航展示时同步调整 `src/components/MainLayout.vue` 菜单。

## 代码风格

- TypeScript 类型明确；公共逻辑抽到 `src/utils/` 或 composable，避免在单文件内堆叠过长。
- 用户可见文案与注释优先使用**简体中文**（与仓库说明一致）。
